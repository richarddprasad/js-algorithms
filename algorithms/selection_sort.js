// TODO: Implement algorithm
// TODO: Test algorithm

// Helper function
function swap(a, b, arr) {
    let t = arr[a];
    arr[a] = arr[b];
    arr[b] = t;
}

function selectionSort(arr) {

}